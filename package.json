{
  "name": "wp_dyn_sponsors",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.html",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bundle": "esbuild ./src/ --bundle --outfile=build/bundle.js --minify",
    "bundle-css": "esbuild ./src/ --bundle --outfile=build/bundle.css --minify",
    "pack": "py bundle.py",
    "publish": "npm run build && npm run bundle && npm run bundle-css && npm run pack"
  },
  "private": true,
  "devDependencies": {
    "typescript": "^5.8.2",
    "esbuild": "^0.25.0"
  }
}
