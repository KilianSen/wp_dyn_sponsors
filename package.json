{
  "name": "wp_dyn_sponsors",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.html",
  "scripts": {
    "bundle": "esbuild ./src/ --bundle --outfile=build/bundle.js --target=esnext --legal-comments=inline",
    "pack": "py bundle.py",
    "publish": "npm run bundle && npm run pack"
  },
  "private": true,
  "devDependencies": {
    "typescript": "^5.8.2",
    "esbuild": "^0.25.0"
  }
}
