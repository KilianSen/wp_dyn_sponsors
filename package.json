{
  "name": "wp_dyn_sponsors",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.html",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bundle": "py bundle.py",
    "publish": "npm run build && npm run bundle"
  },
  "private": true,
  "devDependencies": {
    "typescript": "^5.8.2",
    "esbuild": "^0.25.0"
  }
}
